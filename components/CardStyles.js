import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  card: {
    marginBottom: 20,
    padding: 10,
    borderWidth: 1,
    borderRadius: 10,
    overflow: 'hidden',
  },
  image: {
    width: '100%',
    height: 150,
    marginBottom: 10,
  },
  details: {
    fontSize: 16,
  },
});

export default styles;
